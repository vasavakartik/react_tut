import React from 'react'

export const Stringleng = () => {
    const name = "kartik"
    let length = name.length;
  return (
    <div>Stringlength

        <h1>length is :{length}</h1>
    </div>
  )
}
